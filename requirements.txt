# BASIS Strategy Monitor — Python dependencies (pinned to the versions it was built with).
#
# On the WORK PC, install these OFFLINE from the bundled wheels\ folder (no internet needed):
#     .venv\Scripts\python -m pip install --no-index --find-links wheels -r requirements.txt
# See SETUP_WORK_PC.md for the full, step-by-step setup.
#
# Core (PyPI):
streamlit==1.58.0
pandas==3.0.3
numpy==2.4.6
pyarrow==24.0.0
matplotlib==3.10.9
altair==6.1.0
Jinja2==3.1.6
openpyxl==3.1.5
playwright==1.60.0
pypdfium2==5.9.0      # in-app PDF preview — rasterises report pages; add its wheel to wheels\ for the offline work-PC install
tzdata               # zoneinfo timezone DB (needed on Windows for the Market Hours timeline + COT scheduler)
yfinance==1.5.2      # free Yahoo Finance pulls for the Equities side (quotes/history/fundamentals) — src/yfin.py
streamlit-authenticator==0.4.2  # colleague login (cookie session + bcrypt hashing) — src/auth.py
google-auth==2.55.1             # Gmail OAuth token load/refresh — cot_scheduled_email.py sends
google-api-python-client==2.198.0  # Gmail API client (send/modify) — same, was installed locally
                                     # but never pinned here, so the VPS never had it either

# --- dev-only (NOT needed to run the app; not in the offline wheels\ bundle) -----------
# pytest   # regression suite (tests\, run_tests.bat, pre-push hook) — install manually:
#          #   .venv\Scripts\python -m pip install pytest
#          # left commented out so the offline work-PC install and the VPS sync don't
#          # require a wheel they don't have; run_tests.py records a SKIP without it.

# --- Bloomberg live data ---------------------------------------------------------------
# Included in the offline wheels\ bundle, so the install line above picks them up too.
# blpapi is NOT on PyPI — it lives on Bloomberg's own package index. The directive below
# lets a plain online `pip install -r requirements.txt` find it (the offline
# `--no-index --find-links wheels` install ignores index URLs, so it is unaffected).
--extra-index-url https://blpapi.bloomberg.com/repository/releases/python/simple/
xbbg==1.2.6
blpapi==3.26.4.2
